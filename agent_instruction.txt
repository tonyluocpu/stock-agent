this is the instruction for the stock agent, never delete this or attempt to change unless i explicitly say so

this should be a chatbot format such that it will take in any chat and respond, such as easy questions
- "what is the weather like today"
- "what is the 13th amendement"
Just use common llm functionalities to answer these questions, nothing serious,
there should be some good differtiation if the users want some stock-related funtionality or just 
asking general questions

but specifically this agent will
do the jobs belong if users put in naturral langueg commands similar to these below, the chatbot should not exit until
users exit

Frist layer features(simple):
Use yahoo API to get any hitorical data according to user natural language inputs, 
such as "AAPL weekly data from 2023-2025," then the agent should the data exactly as it is and store in 
data/weekly/beginning and data/weekly/closing and via the symbol name sub directory, all the essential info, such as
price, volumne, etc should be store but not exceeding 5 items
    - this step should use llm to process natural language
    - if user does not specify opening or closing both should be stored
    - should be a single file csv format in creesponding beginning and closing, for example,
    under data/weekly/closing/AAPL and data/weekly/beginning/AAPL
    - name well the csv file such as AAPL_YEARLY_2023_2025.csv
    - ask user if a repetitive file should be replaced/updated if detected there is already one 
    - user can specify time frame such as "2023-2025" or "2014_2017" anything that is natural languge,
    or user could say for 10 years, 12 years, coutning from today's date backward
    - example: "I want weekly data from the 2nd week of 2023 to 5th week of 2025 nvda"
    - example: "apple monthly from january of 2025"
    - example with incorrect spelling: macrosft yestday secondly
    - example with incorrect spelling: tfsla lasy year all year daily
    - IMPORTANT: the agent should differtiate between "and" and "to", when users use and prbabaly it should be 
    multiple files
user could also ask for multiple outputs:
    - aapl and microsfotyearly 2024 only
    - microsf yealy and daily in two files 2024
    - all magnificent 7 last year daily data
    - depending on user input get strong NL processing deciding how many files to generate
DO NOT DEFAULT VALUE ALWAYS CLARIFY

yfinance>=0.2.18
pandas>=1.5.0
requests>=2.28.0
pathlib
argparse
investpy>=1.0.8
polars>=1.0.0
pyarrow>=10.0.0
pytest>=7.0.0
numpy>=1.24.0

# Optional: For free local LLM inference (completely free, no API keys)
# Uncomment the following lines if you want to use local models:
# transformers>=4.30.0
# torch>=2.0.0
# accelerate>=0.20.0
# bitsandbytes>=0.41.0  # For 8-bit quantization on CPU (optional)

# Required: For API server (Flask) - needed for api_interface.py
flask>=2.0.0
flask-cors>=3.0.0